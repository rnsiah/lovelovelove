import 'user_db.dart';

// class UserCart {
//   final dbProvider = DatabaseProvider.dbProvider;

//   Future<int> createCart(OrderItem item) async {
//     final db = await dbProvider.database;
//     var result = await db!.insert(userOrder, item.toJson());
//     return result;
//   }

//   // Future<int> updateCartWithItem(Order orde)
// }
